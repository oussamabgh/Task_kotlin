query getCountryDetail($code:ID!){
    country(code: $code) {
        capital
        name
        emoji
        languages{
            name
        }
        states {
            name

        }
    }
}